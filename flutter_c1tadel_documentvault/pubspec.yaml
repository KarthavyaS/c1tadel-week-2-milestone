name: flutter_c1tadel_documentvault
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  vault_storage: ^2.2.0
  file_picker: ^10.3.3
  path_provider: ^2.1.5
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
